@page "/login"
@using System.ComponentModel.DataAnnotations
@using Microsoft.AspNetCore.Components.Forms
@using TravelOperation.Core.Services
@inject IAuthenticationService AuthService
@inject IJSRuntime JSRuntime
@inject NavigationManager Navigation

<div class="login-container">
    <div class="login-wrapper">
        <div class="login-card">
            <!-- Logo and Title -->
            <div class="logo-section">
                <h1 class="logo-title">CORPORATE TRAVEL SYSTEM</h1>
                <p class="logo-subtitle">EXPENSE MANAGEMENT</p>
            </div>

            <!-- Login Form -->
            <EditForm Model="loginModel" OnValidSubmit="HandleLogin" class="login-form">
            <DataAnnotationsValidator />

            <!-- Email Field -->
            <div class="form-group">
                <label for="email" class="form-label">Email Address</label>
                <div class="input-wrapper">
                    <span class="input-icon">📧</span>
                    <InputText @bind-value="loginModel.Email" 
                               class="form-input" 
                               id="email" 
                               placeholder="Try: admin@corporate.com, sales.manager@wsc.com, or john.doe@wsc.com" 
                               autocomplete="email" />
                </div>
                <ValidationMessage For="@(() => loginModel.Email)" class="validation-error" />
            </div>

            <!-- Password Field -->
            <div class="form-group">
                <label for="password" class="form-label">Password</label>
                <div class="input-wrapper">
                    <span class="input-icon">🔒</span>
                    <input type="@(showPassword ? "text" : "password")" 
                           @bind="loginModel.Password" 
                           class="form-input" 
                           id="password" 
                           placeholder="••••••••" 
                           autocomplete="current-password" />
                    <button type="button" 
                            class="password-toggle" 
                            @onclick="TogglePasswordVisibility">
                        @if (showPassword)
                        {
                            <span>🙈</span>
                        }
                        else
                        {
                            <span>👁️</span>
                        }
                    </button>
                </div>
                <ValidationMessage For="@(() => loginModel.Password)" class="validation-error" />
            </div>

            <!-- Error Message -->
            @if (!string.IsNullOrEmpty(errorMessage))
            {
                <div class="error-banner">
                    ⚠️ @errorMessage
                </div>
            }

            <!-- Sign In Button -->
            <button type="submit" class="signin-button" disabled="@isLoading">
                @if (isLoading)
                {
                    <span class="loading-spinner"></span>
                    <span>Signing In...</span>
                }
                else
                {
                    <span>Sign In</span>
                }
            </button>
        </EditForm>

        <!-- Footer -->
        <div class="login-footer">
            © 2025 Corporate Travel System. All rights reserved.
        </div>
        </div>

        <!-- Test Credentials Hint - Side Panel -->
        <div class="test-credentials">
            <div class="test-title">Quick Test Logins</div>
            <div class="test-item">
                <div class="test-role">
                    <span class="role-badge">Finance</span>
                    <span class="test-email">admin@corporate.com</span>
                    <span class="test-pass">admin123</span>
                </div>
                <button type="button" class="auto-fill-btn" @onclick='() => AutoFillCredentials("admin@corporate.com", "admin123")'>
                    Auto-Fill
                </button>
            </div>
            <div class="test-item">
                <div class="test-role">
                    <span class="role-badge">Owner</span>
                    <span class="test-email">sales.manager@wsc.com</span>
                    <span class="test-pass">manager123</span>
                </div>
                <button type="button" class="auto-fill-btn" @onclick='() => AutoFillCredentials("sales.manager@wsc.com", "manager123")'>
                    Auto-Fill
                </button>
            </div>
            <div class="test-item">
                <div class="test-role">
                    <span class="role-badge">Employee</span>
                    <span class="test-email">john.doe@wsc.com</span>
                    <span class="test-pass">emp123</span>
                </div>
                <button type="button" class="auto-fill-btn" @onclick='() => AutoFillCredentials("john.doe@wsc.com", "emp123")'>
                    Auto-Fill
                </button>
            </div>
        </div>
    </div>
</div>

<style>
    .login-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 50%, #1e40af 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        z-index: 9999;
        padding: 2rem;
    }

    .login-wrapper {
        display: flex;
        gap: 2rem;
        align-items: center;
        max-width: 1000px;
        width: 100%;
    }

    .login-card {
        background: rgba(255, 255, 255, 0.12);
        backdrop-filter: blur(30px);
        border: 1px solid rgba(255, 255, 255, 0.25);
        border-radius: 24px;
        padding: 3rem 2.5rem;
        width: 100%;
        max-width: 480px;
        box-shadow: 0 30px 60px rgba(0, 0, 0, 0.4), 0 0 0 1px rgba(255, 255, 255, 0.1) inset;
        text-align: center;
        animation: fadeInUp 0.6s ease-out;
    }

    @@keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .logo-section {
        margin-bottom: 2.5rem;
        padding-bottom: 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.15);
    }

    .logo-title {
        font-size: 2rem;
        font-weight: 700;
        color: white;
        margin: 0;
        letter-spacing: 0.05em;
        text-shadow: 0 2px 15px rgba(0, 0, 0, 0.4);
        line-height: 1.2;
    }

    .logo-subtitle {
        font-size: 0.875rem;
        color: rgba(255, 255, 255, 0.85);
        margin: 0.75rem 0 0 0;
        font-weight: 400;
        letter-spacing: 0.15em;
        text-transform: uppercase;
    }

    .login-form {
        text-align: left;
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-label {
        display: block;
        color: rgba(255, 255, 255, 0.9);
        font-size: 0.875rem;
        font-weight: 500;
        margin-bottom: 0.5rem;
    }

    .input-wrapper {
        position: relative;
        display: flex;
        align-items: center;
    }

    .input-icon {
        position: absolute;
        left: 1rem;
        font-size: 1.1rem;
        z-index: 1;
        opacity: 0.7;
    }

    .form-input {
        width: 100%;
        padding: 1rem 1rem 1rem 3rem;
        background: rgba(255, 255, 255, 0.12);
        border: 1px solid rgba(255, 255, 255, 0.25);
        border-radius: 12px;
        color: white;
        font-size: 0.95rem;
        transition: all 0.3s ease;
        backdrop-filter: blur(10px);
    }

    .form-input:focus {
        outline: none;
        border-color: rgba(255, 255, 255, 0.5);
        background: rgba(255, 255, 255, 0.18);
        box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.15);
        transform: translateY(-1px);
    }

    .form-input::placeholder {
        color: rgba(255, 255, 255, 0.5);
    }

    .password-toggle {
        position: absolute;
        right: 1rem;
        background: none;
        border: none;
        color: rgba(255, 255, 255, 0.7);
        cursor: pointer;
        font-size: 1.1rem;
        padding: 0;
        z-index: 1;
        transition: color 0.3s ease;
    }

    .password-toggle:hover {
        color: rgba(255, 255, 255, 0.9);
    }

    .validation-error {
        color: #fca5a5;
        font-size: 0.75rem;
        margin-top: 0.25rem;
        display: block;
    }

    .error-banner {
        background: rgba(239, 68, 68, 0.2);
        border: 1px solid rgba(239, 68, 68, 0.3);
        color: #fca5a5;
        padding: 0.75rem;
        border-radius: 8px;
        margin-bottom: 1rem;
        font-size: 0.875rem;
        text-align: center;
    }

    .signin-button {
        width: 100%;
        background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
        color: white;
        border: none;
        padding: 1.125rem;
        border-radius: 12px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        margin-bottom: 1.5rem;
        margin-top: 0.5rem;
        box-shadow: 0 4px 20px rgba(59, 130, 246, 0.35);
        letter-spacing: 0.02em;
    }

    .signin-button:hover:not(:disabled) {
        background: linear-gradient(135deg, #2563eb 0%, #1e40af 100%);
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(59, 130, 246, 0.45);
    }

    .signin-button:disabled {
        opacity: 0.7;
        cursor: not-allowed;
        transform: none;
    }

    .loading-spinner {
        width: 16px;
        height: 16px;
        border: 2px solid rgba(255, 255, 255, 0.3);
        border-top: 2px solid white;
    .test-credentials {
        background: rgba(255, 255, 255, 0.12);
        backdrop-filter: blur(30px);
        border: 1px solid rgba(255, 255, 255, 0.25);
        border-radius: 20px;
        padding: 2rem;
        min-width: 320px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        animation: fadeInRight 0.6s ease-out;
    }

    @@keyframes fadeInRight {
        from {
            opacity: 0;
            transform: translateX(30px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    .test-title {
        color: rgba(255, 255, 255, 0.95);
        font-weight: 600;
        margin-bottom: 1.5rem;
        text-align: center;
        font-size: 1rem;
        letter-spacing: 0.02em;
        padding-bottom: 1rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.15);
    }

    .test-item {
        margin-bottom: 1.25rem;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 12px;
        transition: all 0.3s ease;
    }

    .test-item:hover {
        background: rgba(255, 255, 255, 0.08);
        transform: translateX(4px);
    }

    .test-item:last-child {
        margin-bottom: 0;
    }

    .test-role {
        display: flex;
        flex-direction: column;
        gap: 0.375rem;
    }

    .role-badge {
        color: rgba(255, 255, 255, 0.95);
        font-weight: 600;
        font-size: 0.875rem;
        display: inline-block;
    }

    .test-email {
        color: rgba(255, 255, 255, 0.85);
        font-size: 0.8rem;
        font-family: 'Courier New', monospace;
    }

    .test-pass {
        color: rgba(255, 255, 255, 0.7);
    /* Mobile responsiveness */
    @@media (max-width: 968px) {
        .login-wrapper {
            flex-direction: column;
        }

        .test-credentials {
            width: 100%;
            min-width: auto;
            max-width: 480px;
        }
    }

    @@media (max-width: 480px) {
        .login-container {
            padding: 1rem;
        }

        .login-card {
            padding: 2rem 1.5rem;
            max-width: 100%;
        }

        .logo-title {
            font-size: 1.5rem;
        }

        .logo-subtitle {
            font-size: 0.75rem;
        }

        .test-credentials {
            padding: 1.5rem;
        }
    }   width: 100%;
    }

    .auto-fill-btn:hover {
        background: linear-gradient(135deg, #2563eb 0%, #1e40af 100%);
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4);
    }   margin-left: 0.5rem;
        font-weight: 500;
    }

    .auto-fill-btn:hover {
        background: rgba(59, 130, 246, 0.55);
        color: white;
        transform: translateY(-1px);
        box-shadow: 0 2px 8px rgba(59, 130, 246, 0.3);
    }

    .login-footer {
        color: rgba(255, 255, 255, 0.65);
        font-size: 0.75rem;
        text-align: center;
        margin-top: 0.5rem;
        padding-top: 1rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    /* Mobile responsiveness */
    @@media (max-width: 480px) {
        .login-card {
            margin: 1rem;
            padding: 2rem 1.5rem;
            max-width: 380px;
        }

        .logo-title {
            font-size: 1.5rem;
        }

        .logo-subtitle {
            font-size: 0.75rem;
        }
    }
</style>

@code {
    private LoginModel loginModel = new();
    private bool showPassword = false;
    private bool isLoading = false;
    private string errorMessage = string.Empty;

    public class LoginModel
    {
        [Required(ErrorMessage = "Email is required")]
        [EmailAddress(ErrorMessage = "Please enter a valid email address")]
        public string Email { get; set; } = string.Empty;

        [Required(ErrorMessage = "Password is required")]
        [MinLength(6, ErrorMessage = "Password must be at least 6 characters")]
        public string Password { get; set; } = string.Empty;
    }

    private void TogglePasswordVisibility()
    {
        showPassword = !showPassword;
    }

    private async Task HandleLogin()
    {
        try
        {
            isLoading = true;
            errorMessage = string.Empty;
            StateHasChanged();

            // Use authentication service for login
            if (await AuthService.LoginAsync(loginModel.Email, loginModel.Password))
            {
                // Successful login - redirect to main application
                // Keep loading state active - MainLayout will handle the transition
                Navigation.NavigateTo("/dashboard");
                // Don't set isLoading = false here - let the navigation complete
            }
            else
            {
                errorMessage = "Invalid email or password. Please try again.";
                isLoading = false;
                StateHasChanged();
            }
        }
        catch (Exception ex)
        {
            errorMessage = "An error occurred during login. Please try again.";
            Console.WriteLine($"Login error: {ex.Message}");
            isLoading = false;
            StateHasChanged();
        }
    }

    private async Task HandleForgotPassword()
    {
        await JSRuntime.InvokeVoidAsync("alert", "Please contact your system administrator to reset your password.");
    }

    private void AutoFillCredentials(string email, string password)
    {
        loginModel.Email = email;
        loginModel.Password = password;
        StateHasChanged();
    }

    protected override async Task OnInitializedAsync()
    {
        // Check if user is already logged in
        try
        {
            if (await AuthService.IsAuthenticatedAsync())
            {
                // User already logged in, redirect to main app
                Navigation.NavigateTo("/dashboard");
            }
        }
        catch
        {
            // Stay on login page
        }
    }
}