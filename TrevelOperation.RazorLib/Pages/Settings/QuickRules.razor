@page "/settings/rules"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // Redirect to TransformationRules page - these are the same feature
        NavigationManager.NavigateTo("/settings/transformation-rules", replace: true);
    }
}
